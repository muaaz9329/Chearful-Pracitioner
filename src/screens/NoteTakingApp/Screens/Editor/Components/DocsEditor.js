import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const DocsEditor = ({route,navigation}) => {
  return (
    <View>
      <Text>DocsEditor</Text>
    </View>
  )
}

export default DocsEditor

const styles = StyleSheet.create({})